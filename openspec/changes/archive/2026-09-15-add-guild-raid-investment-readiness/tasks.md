## 1. Sourced Data Authoring

- [x] 1.1 For the 5 bosses `cognitae.app/meta` covers (Lion, Silent King, Mortarion, Ghazghkull, Belisarius Cawl), resolve every named archetype's core/flex roster to catalog character ids, including confirming the Gulgortz identification against a portrait/source before authoring it. Resolved: the catalog character `orksWarboss` has `name: "Gulgortz"` directly (confirmed via `units-orks.json`) — not "Ironskull"; the earlier session's tentative name was a misremembering, not a wrong identification. Rosters were re-extracted from the live page's image `alt` text (portrait names + titles) via the DOM, not from prose summaries, after an initial `WebFetch` pass was found to fabricate/misattribute rosters across sections.
- [x] 1.2 For the remaining 7 bosses, resolve `terminusmaximus.com`'s Meta/Alternate roster character names to catalog character ids (unchanged from the original plan). Carried forward verbatim from the prior `guild-raid-meta.json` (already-resolved ids), only `efficiency` added.
- [x] 1.3 Resolve `cognitae.app/side-bosses`' 14 named primes' rosters to catalog character ids, and resolve every prime across all 12 bosses to `raid-bosses` prime `unitSetId`s. Correction to the task's premise: the true count is **29** primes (`MiniBoss`/`Minion`-keyed unit sets), not 24 — see report. All 15 prime cards (14 names; Lion has 2) were identified via portrait filename (`ui_image_portrait_{slug}`), which directly embeds the target `raid-bosses` unit-set suffix, giving an unambiguous, non-inferred mapping.
- [x] 1.4 Compute `efficiency` per the design doc's two derivations, anchoring each boss/prime's weakest included recommendation at `1.0`. The design doc's 8-row table included Silent King under the replay-ratio method, but Silent King is cognitae-covered — its efficiency was (re)computed via the tier-floor method instead; the other 7 terminusmaximus-only values were reused as-is.
- [x] 1.5 Split `Data/guild-raid-meta.json` into `Data/guild-raid-meta/guild-raid-comps.json` and one `Data/guild-raid-meta/guild-raid-meta-boss-{n}-{slug}.json` per boss, authoring `bosses[]` (variable-length `recommendations`, `kind` as archetype slug, `efficiency`, `primeUnitSetIds`) and `primes[]` (for the 14 sourced primes) in place of the prior placeholder content. Deleted the old single `guild-raid-meta.json`. Generated programmatically from the resolved source data (a scratch Python script) to keep the 53 recommendations across 12 files internally consistent; validated afterward against the live catalog (every `heroId`/`mowId`/`compId`/`unitSetId` resolves, all recommendation `id`s are globally unique, no duplicate `kind` within a group) — see report for the modeling choices made where cognitae's Core+Flex breakdown doesn't map 1:1 onto a fixed 5-hero roster.

## 2. Models, Loading, and Denormalization

- [x] 2.1 Change `Kind` from a closed set to a plain non-empty string on the raw/served recommendation records; add `Efficiency` to the recommendation records; add `PrimeUnitSetIds` to the boss-group records; add new raw/served records for `primes[]` reusing the recommendation shape. (`Kind` was already a plain string at the model layer from the prior `add-guild-raid-variant-rules` work — the closed set lived only in `Validation/GuildRaidMetaValidation.cs`, removed in 3.1.)
- [x] 2.2 Extend `GameCatalogLoader`'s Guild Raid Meta loading to merge `guild-raid-comps.json` plus every per-boss file (each now possibly contributing to both `bosses[]` and `primes[]`) into one raw dataset, mirroring `LoadRaidBossRawData`'s per-file merge and duplicate-key failure behavior, and verify a loader test covers the merge. Registered `GuildRaidMetaComps` and `GuildRaidMetaBossGroups` (mirroring `RaidBossGroups`) in `GameCatalogDatasets`; added `LoadGuildRaidMetaRawData()`.
- [x] 2.3 Extend `GuildRaidMetaDenormalizer` to project `efficiency`, `primeUnitSetIds`, and `primes[]` through unchanged, and verify denormalizer tests assert order preservation for all three, including a boss with more than two recommendations.

## 3. Validation

- [x] 3.1 Extend `GuildRaidMetaValidation` to require positive `efficiency`, unique `kind` within each boss/prime group (no longer restricted to `meta`/`alternate`), and to resolve every `primeUnitSetIds` entry and every `primes[].primeUnitSetId` against `raid-bosses` prime records, and verify validation tests cover a non-positive efficiency value, a duplicate `kind` within one group, and an unresolved prime id. Removed the closed `ValidGuildRaidMetaRecommendationKinds` set and its `InvalidRecommendationKind` check.

## 4. Companion and Repository Gates

- [x] 4.1 Update `GameCatalogLoaderTests`, `GuildRaidMetaDenormalizerTests`, and `GuildRaidMetaValidationTests` for the new fields, the per-file merge, the widened `kind`, and `primes[]`, and verify they assert the sourced data's shape (not the prior placeholder). Also extended `GameCatalogSnapshotTests` (API-level) for `efficiency`/`primeUnitSetIds`/`primes[]` and a >2-recommendation boss.
- [x] 4.2 Promote the API manifest snapshot and confirm only the `guild-raid-meta` dataset hash and the aggregate source hash changed. Diffed received vs. verified before promoting: only that hash line changed.
- [x] 4.3 Run `dotnet format`, `dotnet build`, and `dotnet test` and verify all three succeed. All three pass clean across the whole solution (350 tests: 122 GameCatalog + 224 Api + 4 Persistence.IntegrationTests).
